node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# This is a Bun project — bun.lock is the only lockfile. Never commit an npm lockfile.
package-lock.json

# Registry build output — generated by `bun run build:registry`, deployed via CI.
public/
